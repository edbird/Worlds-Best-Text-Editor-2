#ifndef FONT_LOAD_PROPERTIES_H
#define FONT_LOAD_PROPERTIES_H

#include <string>

// TODO: remove
/*
class FontLoadProperties
{

public:

    FontLoadProperties(const std::string& fontFullPath, const int fontSize)
        : mFontFullPath(fontFullPath)
        , mFontSize(fontSize)
    {
    }

    std::string getUniqueKey() const
    {
        return mFontFullPath + std::to_string(mFontSize);
    }

private:

    std::string mFontFullPath;
    int mFontSize;

};
*/

#endif // FONT_LOAD_PROPERTIES_H
